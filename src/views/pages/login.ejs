<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="manifest" href="/manifest/manifest.json">
    <link rel="shortcut icon" href="/manifest/icon512_maskable.png" type="image/x-icon">
    <title>Login Google + Sheets</title>
    <style>
        :root{--accent:#1e90ff;--muted:#9aa7bf}
        html,body{height:100%;margin:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,sans-serif}
        body{display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#0f172a,#0b2545);color:#fff}
        .login-card{width:100%;max-width:420px;padding:32px;border-radius:12px;background:rgba(255,255,255,0.04);backdrop-filter:blur(6px);box-shadow:0 10px 30px rgba(2,6,23,0.6);text-align:center}
        h1{margin:0 0 18px;font-size:1.4rem;font-weight:600}
        .btn{display:inline-flex;align-items:center;gap:10px;padding:10px 16px;border-radius:8px;border:0;background:linear-gradient(90deg,var(--accent),#4aa3ff);color:#fff;font-weight:600;cursor:pointer;font-size:1rem;transition:transform .12s ease,box-shadow .12s ease;box-shadow:0 6px 18px rgba(30,144,255,0.18)}
        .btn:hover{transform:translateY(-2px);box-shadow:0 10px 30px rgba(30,144,255,0.2)}
        a{text-decoration:none}
        @media (max-width:420px){.login-card{margin:16px;padding:20px}}
    </style>
</head>

<body>
    <main class="login-card" role="main">
        <h1>Login com Google</h1>
        <a href="/auth/google" aria-label="Entrar com Google">
            <button class="btn" type="button">
                Logar com Google
            </button>
        </a>
    </main>
</body>

</html>